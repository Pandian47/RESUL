/**
 * Advance search defaults — no Constants/AdvanceSearch barrel import (prod TDZ in heavy chunks).
 */
export const DEFAULT_ADVANCE_SEARCH_SORT_BY_ID = 1;

export const DEFAULT_SORT_TYPE_ADVANCE_FILTER_VALUE = Object.freeze({
    displayValue: 'Created: Newest first',
    rawValue: Object.freeze({ id: 1, name: 'Created: Newest first' }),
});
