import FormGenerator from '../FormTypes/FormGenerator';

const Subscription = ({ setCurrentTab }) => {
    return <FormGenerator tag="KYC" setCurrentTab={setCurrentTab} />;
};

export default Subscription;
