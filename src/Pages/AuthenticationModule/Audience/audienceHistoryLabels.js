/** Sync history / import summary labels — no Placeholders barrel import (prod TDZ). */
export const AUDIENCE_HISTORY_LABELS = Object.freeze({
    AUDIENCE_IMPORTED_WITH_WARNINGS: 'Audience imported with warnings',
    AUDIENCE_NOT_IMPORTED: 'Audience not imported',
    DUPLICATE_RECORDS_NOT_IMPORTED: 'Duplicate records not imported',
    AUDIENCE_IMPORT_SUMMARY: 'Audience import summary',
});
