import { ENTER_SINGLE_WORD } from 'Constants/GlobalConstant/ValidationMessage';
export const _isSingleWord = (value) =>
 {  
    // console.log("Value received:", value);  
    // return  !Array.isArray(value) && value?.split(' ')?.length > 1 ? ENTER_SINGLE_WORD : true;
    return    true;
 }
 